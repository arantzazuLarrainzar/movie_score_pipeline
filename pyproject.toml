[project]
name = "movie_score_pipeline"
version = "0.1.0"
description = "Data Pipeline of movie information coming from three different providers."
readme = "README.md"
requires-python = "==3.8.10"
dependencies = [
    "pandas>=2.0.3",
]
